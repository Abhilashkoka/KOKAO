# KOKAO

KOKAO (internal codename SocialForge; code packages keep the `@workspace/socialforge` name) is a multi-tenant SaaS web app for AI-powered social media content: it generates on-brand captions and images, organizes a content library, manages brand kits, schedules posts, and connects social accounts (Instagram/Facebook/LinkedIn/YouTube). Built to scale to 5K-20K users with per-tenant subscription quotas (free/pro/business).

## Run & Operate

- `pnpm --filter @workspace/api-server run dev` — run the API server
- `pnpm --filter @workspace/socialforge run dev` — run the web frontend
- `pnpm run typecheck` — full typecheck across all packages
- `pnpm run build` — typecheck + build all packages
- `pnpm --filter @workspace/api-spec run codegen` — regenerate API hooks and Zod schemas from the OpenAPI spec
- `pnpm --filter @workspace/db run push` — push DB schema changes (dev only)
- Required env (asserted at boot in production by `lib/assertEnv.ts`): `DATABASE_URL`, `SESSION_SECRET`, Clerk keys (`CLERK_SECRET_KEY`, `CLERK_PUBLISHABLE_KEY`, `VITE_CLERK_PUBLISHABLE_KEY`), object storage vars (`PUBLIC_OBJECT_SEARCH_PATHS`, `PRIVATE_OBJECT_DIR`, `DEFAULT_OBJECT_STORAGE_BUCKET_ID`), OpenAI access via integration proxy. Optional: `SUPERADMIN_EMAILS` (comma-separated cross-tenant admins), `CREDENTIALS_ENCRYPTION_KEY` (dedicated at-rest key; falls back to `SESSION_SECRET`), `LINKEDIN_CLIENT_ID`/`LINKEDIN_CLIENT_SECRET`, SendGrid connector.

## Stack

- pnpm workspaces, Node.js 24, TypeScript 5.9
- API: Express 5
- DB: PostgreSQL + Drizzle ORM
- Validation: Zod (`zod/v4`), `drizzle-zod`
- API codegen: Orval (from OpenAPI spec)
- Auth: Replit-managed Clerk (cookie-based sessions on web)
- Frontend: React 19 + Vite + wouter + TanStack Query + shadcn/ui
- AI: OpenAI via Replit AI integration proxy (captions + images)
- Build: esbuild (CJS bundle)

## Where things live

- API contract (source of truth): `lib/api-spec/openapi.yaml` — change here, then run codegen
- Generated API hooks/schemas: `lib/api-client-react/src/generated/` (import via `@workspace/api-client-react`)
- DB schema (source of truth): `lib/db/src/schema/` (tenants, brandKits, brandKitVersions, brandAssets, brandPreferences, contentItems, scheduledPosts, connectedAccounts, usageEvents)
- Brand kit module: payload type `lib/db/src/schema/brandKitPayload.ts` (`BrandKitPayload`, versioned-JSON source of truth). Server helpers `artifacts/api-server/src/lib/brandKit/{defaults,service,selection,draft}.ts` — `loadActivePayload`/selection load only the current tenant's non-archived kits. Routes `routes/{brandKits,brandPreferences,onboarding}.ts` are SESSION-scoped (NO tenantId in URL — deliberate IDOR avoidance; scope every query by `req.tenantId`). Frontend `pages/brand-kits.tsx` + `components/onboarding-wizard.tsx` (gated on `me.brandOnboardingComplete === false`).
- API routes: `artifacts/api-server/src/routes/` — auth gating lives in `routes/index.ts`
- Tenant provisioning: `artifacts/api-server/src/middlewares/requireTenant.ts`
- Plan limits / quota helpers: `artifacts/api-server/src/lib/plans.ts` (async DB-backed store: `plan_settings` table overrides `DEFAULT_PLANS`, 30s cache, falls back to defaults on DB read failure without caching the degraded result; `getPlanLimits` is async — await it), `lib/usage.ts`. Superadmin plan editor: `PUT/POST/DELETE /admin/plans[...]` in `routes/admin.ts` — dynamic catalog: rows in `plan_settings` override defaults, add custom plans, or (archived=true) delete a built-in default; "free" is the undeletable signup fallback and in-use plans can't be deleted; -1 = unlimited; audits as `plan_edit`/`plan_create`/`plan_delete` with null target. Tenant plan writes validate against the live catalog (no enum). UI card `PlansCard` in `pages/admin/`.
- Superadmin: allowlist `artifacts/api-server/src/lib/superadmins.ts`, grantable DB flag `tenants.isSuperadmin`, gate `middlewares/requireSuperadmin.ts`, routes `routes/admin.ts` (owner-only `PATCH /admin/tenants/:id/superadmin`), live verified-email helper `lib/clerkUser.ts`, page `artifacts/socialforge/src/pages/admin/`
- Notification preferences (control layer): catalog `artifacts/api-server/src/lib/notificationCatalog.ts` (per-type defs; currently `social_connection_failed`), resolution `lib/notificationSettings.ts` (`resolveEffective`/`getEffectiveSetting` fold global policy + tenant preference). Global policy is superadmin-owned (`notification_policies`: enabled + emailPolicy optional|forced|off); each tenant's own choice lives in `notification_preferences` (inApp/email). Team members/admins working in someone else's workspace get MEMBER-scoped rows instead (`member_notification_preferences`, keyed tenantId+clerkUserId+type): `/notification-settings` reads/writes the member table when `req.memberRole !== "owner"` (response carries `scope: workspace|member`), and `notifyTeamMemberLeft` skips emailing an admin who opted out (forced policy overrides; owner/other admins unaffected). Tenant routes `routes/notificationSettings.ts` (`/notification-settings` GET/PUT), admin routes in `routes/admin.ts` (`/admin/notification-policies` GET/PUT). Dispatch (`lib/notifications.ts`) consults `getEffectiveSetting` before writing/emailing. Policy authority is enforced SERVER-SIDE (forced/off/disabled override tenant choice); never trust the client. UI: Settings "Notifications" tab (`artifacts/socialforge/src/components/notification-settings.tsx`), admin "Notification policies" card (`pages/admin/`).
- Breakage notifications: choke point `artifacts/api-server/src/lib/notifications.ts` (`notifySocialConnectionFailed`) records a deduped in-app notification AND emails the tenant's verified address (via `lib/clerkUser.ts` `fetchVerifiedEmail`) on a fresh breakage. Email goes through the SendGrid connector at runtime (`lib/email.ts`, no hardcoded creds); it is a best-effort side channel that no-ops safely when SendGrid isn't connected. Reconnect link uses `REPLIT_DOMAINS`.
- OAuth state (shared): `artifacts/api-server/src/lib/oauthState.ts` — `signOAuthState`/`verifyOAuthState`/`randomNonce`. HMAC-signed, tenant-bound, TTL'd `state` carrying an opaque `data` field (PKCE verifier for X, random nonce for LinkedIn). Used by `routes/{twitter,linkedin}.ts`; do not re-implement per platform.
- Social credentials: encryption helper `artifacts/api-server/src/lib/secretCrypto.ts` (AES-256-GCM; ENCRYPT with `CREDENTIALS_ENCRYPTION_KEY` when set, else `SESSION_SECRET`; DECRYPT is dual-read — tries the dedicated key then falls back to `SESSION_SECRET`, so enabling the dedicated key does not brick existing creds; they re-encrypt under it on next write. Payloads are version-prefixed `v1:` and legacy unprefixed payloads still decrypt), Meta API helper `lib/metaApi.ts`, credential CRUD `routes/credentials.ts` (admin `/admin/platform-credentials/meta`; tenant `/social-credentials/{facebook,instagram}`), publishing `routes/meta.ts` (`/content/:id/publish-facebook`, `/content/:id/publish-instagram`), app-level creds schema `lib/db/src/schema/appCredentials.ts`, tenant creds on `connectedAccounts` (`encryptedCredentials`/`verifyStatus`). Meta secrets go in headers/POST body, never URLs. LinkedIn app creds: superadmin `GET/PUT /admin/platform-credentials/linkedin` (encrypted `app_credentials` row, provider "linkedin"); `routes/linkedin.ts` `getCredentials()` reads the DB row first and falls back to `LINKEDIN_CLIENT_ID`/`LINKEDIN_CLIENT_SECRET` env vars. Admin UI card `LinkedinCredentialsCard` in `pages/admin/`.
- App branding (white-label): singleton config `lib/db/src/schema/appBrandSettings.ts` (id=1; appName, logoUrl, iconUrl, primaryColor, backgroundColor, all nullable = fall back to bundled KOKAO default). Public upload helper `artifacts/api-server/src/lib/objectStorage.ts` `getPublicBrandUploadURL()` (mints PUT under public `brand/<uuid>`, served via `/api/storage/public-objects/brand/<uuid>`). Routes `routes/appBrand.ts`: `GET /app-brand` PUBLIC (mounted before `requireTenant` so landing/auth can read it), `PUT /app-brand` + `POST /app-brand/upload-url` superadmin-only. Frontend `src/lib/brand.tsx` (`BrandProvider`/`useBrand`: hex→HSL theme vars, favicon+title, logo/appName with KOKAO fallback) wraps the app; superadmin editor `src/pages/app-branding.tsx` (nav label "Branding", route `/app-brand`). Consumed by `components/layout.tsx`, `pages/landing.tsx`, `pages/auth.tsx`.
- Team add-on: schema `lib/db/src/schema/team.ts` (tenantMembers, teamInvites, seatRequests) + `tenants.seatLimit` + `planSettings.teamSeats`. Server lib `artifacts/api-server/src/lib/team.ts` (effective limit = tenant.seatLimit ?? plan.teamSeats, 0 = off; seats used = owner + members + pending invites). Invite auto-accept lives in `middlewares/requireTenant.ts` (verified email match, seat check); management routes `routes/team.ts` gated by `middlewares/requireWorkspaceAdmin.ts`; removing a member (DELETE /team/members/:id) also revokes any lingering PENDING invite for that email — rejoin requires a deliberate new invite sent after removal; team-management alerts (`team_member_left`, `team_member_removed`, `seat_request_decided`) email the owner AND every admin member's verified Clerk address via the shared `fetchWorkspaceEmailRecipients`/`emailWorkspaceRecipients` helpers in `lib/notifications.ts` (actor/leaver excluded, deduped, best-effort per recipient), each gated by the workspace's effective settings for that type; `social_connection_failed` stays owner-only by design; superadmin seat-request review in `routes/admin.ts` (approve writes `tenants.seatLimit`, audited, notifies tenant). UI: Settings "Team" tab (`components/team-settings.tsx`), admin "Seat requests" card (`pages/admin/`). CAUTION: with members, `req.tenantId` can be another user's workspace — never write owner-identity tenant columns unless `req.memberRole === "owner"` and scoped by clerkUserId.
- Design skill (canvas-design): prompt template `artifacts/api-server/src/skills/canvasDesign.ts`; service `lib/designSkill.ts` (global switch from `design_skill_settings` singleton, per-tenant override `tenants.designSkillEnabled` null=follow global; `buildDesignedImagePrompt` runs a 2-step design pass over the user prompt + brand kit, fails soft to the plain prompt). Wired into `/ai/generate-image` in `routes/ai.ts`. Superadmin toggles: `GET/PUT /admin/design-skill` + `PATCH /admin/tenants/:id/design-skill` in `routes/admin.ts` (audited as `design_skill_change`); UI card `DesignSkillCard` + per-tenant Default/On/Off select in `pages/admin/`. Studio blocks image generation when a tenant has multiple brand kits and none is selected (single kit auto-resolves server-side).
- Image generation providers: catalog `artifacts/api-server/src/lib/imageGen/` (index.ts selection + key resolution; providers/ openaiBuiltin (proxy, no key) | gemini (incl. Nano Banana Pro gemini-3-pro-image-preview) | bfl (Black Forest Labs FLUX, async submit+poll, BFL_API_KEY) | stability | replicate | openaiCompatible (custom baseUrl+model); catalog entries may list suggested modelOptions shown as a picker in the admin card). Selection singleton `lib/db/src/schema/imageGenSettings.ts` (provider/model/customBaseUrl). Keys stored encrypted in `app_credentials` rows `imagegen_<id>` (DB key wins over env secret GEMINI_API_KEY/STABILITY_API_KEY/REPLICATE_API_TOKEN/CUSTOM_IMAGE_API_KEY; clients only see `keySource`). `/ai/generate-image` calls `generateImage()` (NotConfigured→503, ProviderError→502). Admin routes GET/PUT /admin/image-gen-settings + PUT/DELETE /admin/image-gen-providers/:id/key in `routes/admin.ts` (audited as `imagegen_provider_change`/`imagegen_key_change`); UI card `ImageGenProviderCard` in `pages/admin/ai-tab.tsx`. Custom provider is SSRF-guarded (https-only, `assertPublicHost`, manual redirects) for both the endpoint and any provider-returned image URL.
- Voice notes / ASR (speech-to-text): provider catalog `artifacts/api-server/src/lib/asr/` (index.ts catalog + selection + key resolution; providers/ groq | openaiWhisper (built-in proxy, no key) | deepgram | assemblyai; default "groq"). Selection singleton `lib/db/src/schema/asrSettings.ts`. API keys: superadmin enters them in the admin "Speech-to-Text Provider" card — stored AES-encrypted in `app_credentials` rows `asr_<providerId>` (DB key wins; `<PROVIDER>_API_KEY` env secret is the fallback; keys are never returned to clients, only `keySource`). Routes: POST /ai/transcribe (multer, 15MB, unmetered) in `routes/ai.ts`; GET/PUT /admin/asr-settings + PUT/DELETE /admin/asr-providers/:id/key in `routes/admin.ts` (audited as `asr_provider_change`/`asr_key_change`). UI: mic button `components/voice-note-button.tsx` in the web Studio and `artifacts/mobile/components/VoiceNoteButton.tsx` (expo-audio native + MediaRecorder web) in the mobile Studio; `AsrProviderCard` in `pages/admin/`. Tests: `lib/asr/asrKeys.test.ts`.
- Taste memory (style learning): table `lib/db/src/schema/tasteProfiles.ts` (one row per tenant, jsonb payload); lib `artifacts/api-server/src/lib/tasteMemory.ts` (signal weights saved=1/scheduled=2/published=3/discarded=1, 5%/week decay on exemplar ranking, transactional SELECT FOR UPDATE writes); hooks in `routes/{content,schedules,meta,linkedin,twitter,threads}.ts`; routes `routes/tasteProfile.ts` (GET/PUT/DELETE /taste-profile + POST /taste-profile/signal); AI wiring in `routes/ai.ts` (soft guidance after brand kit — brand rules and the explicit prompt always win); UI Settings "Style memory" tab (`components/taste-memory-settings.tsx`), studio fires the "saved" signal on Save to Library.
- Billing (Razorpay): schema `lib/db/src/schema/billing.ts` (subscriptions, credit_packs, credit_balances, credit_ledger with unique `razorpay_order_id`, razorpay_events for webhook idempotency) + `planSettings.priceInr`/`razorpayPlanId`. Client `artifacts/api-server/src/lib/razorpay.ts` (fetch-based; encrypted `app_credentials` row "razorpay" with keyId/keySecret/webhookSecret, superadmin card; HMAC signature verification for payments/subscriptions/webhooks). Credits `lib/credits.ts` (transactional SELECT FOR UPDATE grant/spend; grants idempotent per Razorpay order id via ledger unique index — Drizzle wraps pg errors so dedupe walks `error.cause` for 23505; balances never go below zero and the LEDGER RECORDS THE APPLIED DELTA, not the requested one, so ledger totals always reconcile with the balance). Tenant routes `routes/billing.ts` (`/billing` overview, subscribe/verify-subscription/cancel, switch-payg, purchase-credits/verify-purchase — all writes OWNER-only via `req.memberRole`; verify paths re-fetch the canonical Razorpay entity and require status `paid`/`active|authenticated`, never trusting the browser). Admin plan override wins: `tenants.planOverriddenAt` (set by PATCH /admin/tenants/:id, which 409s on active subscriptions unless `confirmActiveSubscription`) blocks webhook plan sync; cleared by the tenant's own billing actions. Webhook `routes/razorpayWebhook.ts` — PUBLIC (mounted before requireTenant), signature-gated on rawBody, event-id deduped in `razorpay_events`; subscription lapse (cancelled/expired/completed/halted) downgrades the tenant to FREE, and only after the paid period end has passed (early terminal events defer); `payment.captured` credit backstop fetches the order from Razorpay and cross-checks purpose/tenant/pack/amount before granting (never trusts payload notes). Plan price sync: saving `priceInr` on a plan auto-creates a Razorpay Plan (`lib/plans.ts` + admin routes). Quotas: AI endpoints spend plan quota first, then credits; 402 only when both are gone; "payg" is a built-in zero-quota plan powered by credits. UI: Settings "Billing" tab (`components/billing-settings.tsx`, Razorpay Checkout), studio strip shows credits, admin cards for credentials/credit packs/credit grants (audited). Prices are stored in PAISE. Tests: `lib/credits.test.ts`, `routes/razorpayWebhook.test.ts` (own express app with rawBody verify + signed HMAC; mocks `fetchRazorpayOrder`).
- Analytics (consent-gated): schema `lib/db/src/schema/analytics.ts` (analytics_events with per-column consent-gated context, user_consents per clerkUserId) + `usage_events` extended with requestBytes/responseBytes/durationMs/model/funding. Server lib `artifacts/api-server/src/lib/analytics.ts` (consent flags, ANONYMOUS_ALLOWED_EVENTS lifecycle allowlist, geo-IP from request headers, anonymous→user merge). Ingest `routes/analyticsIngest.ts` — PUBLIC POST /analytics/events, but consent is enforced SERVER-SIDE from STORED consent (no analytics consent = whole batch dropped; category opt-outs null device/carrier/location columns regardless of payload; anonymous senders get lifecycle events only with all gated fields stripped). Consent CRUD `routes/consent.ts`; reports `routes/analytics.ts` (8 endpoints, superadmin = platform-wide + ?tenantId drilldown, workspace owner/admin = own tenant only, members 403). Web tracker `artifacts/socialforge/src/lib/analytics.ts` (batch queue, 30-min sessions, UTM first-touch, sendBeacon on hide); mobile mirror `artifacts/mobile/lib/analytics.ts` + `components/AnalyticsTracker.tsx` (Clerk bearer token, expo-device/cellular/network/battery; device GPS only for precise opt-in — coarse comes from server geo-IP). UI: `/analytics` page (`src/pages/analytics/`, 8 tabs via ?tab=), consent prompt in onboarding + Settings. Tests: `routes/analytics.test.ts`.
- Frontend: `artifacts/socialforge/src/`

## Architecture decisions

- Multi-tenancy without Clerk orgs: each `clerkUserId` is its own tenant, auto-provisioned on the first authenticated request by `requireTenant`. No manual signup/tenant-creation step.
- Auth is gated ONCE in `routes/index.ts`: public routers (health, storage, plans) are mounted first, then `router.use(requireTenant)`, then the protected routers. Do NOT add `requireTenant` per-router.
- Quotas: AI caption/image endpoints enforce per-tenant monthly limits and return HTTP 402 when exceeded; usage is metered via `usageEvents`.
- AI is owner/tenant-configurable via `tenant.aiModel`. Images go to object storage; endpoints return both a stored `imagePath` and `b64Json` for instant preview.
- Web auth uses Clerk session cookies (same-origin via proxy) — the frontend does NOT use `setAuthTokenGetter` or a custom API base URL.
- Cross-tenant superadmin is BOTH an email allowlist (env-only via `SUPERADMIN_EMAILS`, comma-separated; NO hardcoded emails in source — an unset var means no allowlisted admins) AND a grantable per-tenant DB flag (`tenants.isSuperadmin`). Effective superadmin = DB flag OR allowlisted email. The `/admin/*` gate (`requireSuperadmin`) trusts the DB flag as a fast-path (read fresh each request, so revoke is immediate), otherwise falls back to checking the user's LIVE verified Clerk email — only verified emails count, and it fails closed (403) on any Clerk error. The cached `tenants.email` column is only a UI hint (drives `/me` `isSuperadmin`/`isOwner` and the nav link); it is never the security boundary. Do not gate authorization on the cached column.
- Role management (grant/revoke superadmin) is OWNER-ONLY: `PATCH /admin/tenants/:id/superadmin` independently re-checks that the ACTOR is allowlisted via their LIVE verified email (a merely granted superadmin cannot mint/remove superadmins) and rejects writes whose TARGET is allowlisted (owners are permanent, shown as "Owner" with a locked toggle). `/me` exposes `isOwner` so the UI disables role controls for non-owner superadmins.
- Frontend admin page denies access when ANY admin endpoint returns 403 (authoritative), not just on the cached `me.isSuperadmin` — React Query serves that stale after a live revoke, so a revoked user would otherwise keep seeing the dashboard.
- AI assists in `routes/ai.ts`: `/ai/suggest-topics` and `/ai/summarize-url` are UNMETERED (helpers, no quota). `/ai/generate-campaign` is metered as 1 caption per requested platform — it pre-checks quota (402 if `usage.captions + platforms.length` would exceed the plan) and records one caption usage per platform on success. Campaign returns caption+hashtags+imagePrompt per platform but does NOT generate images server-side; the UI calls the existing `/ai/generate-image` per platform on demand.
- Object storage is tenant-scoped by KEY, not by ACL metadata: uploads are minted under `/objects/<tenantId>/uploads/<uuid>` and every read/publish path (`getObjectEntityFile`/`getSignedDownloadURL` in `lib/objectStorage.ts`) asserts the `/objects/<tenantId>/` prefix matches `req.tenantId`, throwing `ObjectNotFoundError` (404) on mismatch. There is no ACL module anymore; `downloadObject` takes an explicit `{ isPublic }` flag (public storage route passes `true`). This is the security boundary — never read cross-tenant objects by trusting a client-supplied path. Non-mocked enforcement guard: `lib/objectStorage.test.ts`.
- Production hardening (enforced only when `NODE_ENV=production`, no-op in dev/test): `assertRequiredEnv()` at boot (`index.ts` → `lib/assertEnv.ts`) fails loud if core env is missing; CORS is an allowlist built from `REPLIT_DOMAINS` (`app.ts`, replaces wildcard credentialed CORS; no-Origin requests allowed); `helmet()` + `trust proxy=1` + `express-rate-limit` (`middlewares/rateLimit.ts`): global 300/min, `aiLimiter` 30/min on `/ai`, `sensitiveLimiter` 20/min on `/social-credentials`/`/twitter`/`/linkedin` — all skip when `NODE_ENV=test`.
- SSRF guard: `/ai/summarize-url` fetches arbitrary user URLs server-side, so it is hardened in `routes/ai.ts` — `assertPublicHost` blocks IP-literal and DNS-resolved private/loopback/link-local/CGNAT/multicast/reserved ranges (IPv4 + full IPv6 incl. all IPv4-mapped/compatible forms via `ipv6ToBytes`), blocks `localhost`/`.local`/`.internal`, strips IPv6 brackets, and fails closed. `safeFetch` uses `redirect:"manual"` and re-validates the host on every hop. Response body is streamed and capped (`MAX_FETCH_BYTES`), content-type is allowlisted, and the timeout is cleared in `finally`. Residual risk: DNS-rebinding TOCTOU (validation resolves the host, then `fetch` resolves again) is accepted given the request timeout and small response cap; tighten with IP-pinned connect/egress proxy if needed.

## Product

- AI content studio: generate captions and images from a prompt, optionally tied to a brand kit. Single-result generations auto-save to the library as a draft (studio tracks the draft id and updates it in place; "Save to Library" accepts, "Discard" deletes; on update failure only a 404 triggers re-create to avoid duplicate drafts). A header strip shows live captions/images remaining this month (the `me` query is invalidated after each metered generation); ideas/research/briefs are unmetered. Also: topic ideation (niche -> 5 ideas), article-URL -> brief (fetch + summarize to {title, summary}), and multi-platform campaign generation (one brief -> per-platform caption + hashtags + image prompt).
- Content library: save, edit, delete content items (draft/scheduled/published).
- Brand kits: multiple brands per tenant with a versioned-JSON payload (identity, logos, colors, typography, voice, visual style, layout tokens, channel rules, brand controls) as the source of truth. One default brand; editing creates a new activated version; best-effort AI draft from URL/notes; skippable first-login onboarding wizard.
- Scheduling: plan posts on a calendar. IMPORTANT: this RECORDS scheduled posts only — there is NO executor/cron that publishes them at the scheduled time. Publishing is manual from the Content Library.
- Connected accounts: Instagram/Facebook/LinkedIn/YouTube/Threads records. Facebook Page + Instagram Business use a real encrypted credential framework (admin sets Meta App ID/Secret once; each tenant enters own Page token/ID + IG account ID, auto-tested on save); LinkedIn uses real OAuth; Threads uses real OAuth (`routes/threads.ts`) with superadmin-managed Threads App ID/Secret (`/admin/platform-credentials/threads`, no env fallback) and long-lived tokens with auto-refresh — long captions publish as a 500-char reply-chained thread. Publish to these from the Content Library once verified.
- Settings: workspace name, AI model, plan; view available plans.
- Admin dashboard (superadmin only, `/admin`): platform stats, all-tenants table with counts/usage, per-tenant plan changes, and (owners only) grant/revoke of the superadmin role per tenant.

## User preferences

- No emojis in the UI.

## Gotchas

- Image display: render stored images via `/api/storage${imagePath}` (imagePath has the form `/objects/<tenantId>/uploads/<uuid>`). Objects are tenant-namespaced; a path whose `/objects/<tenantId>/` prefix does not match the caller's tenant is rejected with a 404. Legacy non-namespaced `/objects/uploads/...` paths (from before this scheme) will NOT resolve.
- After editing `openapi.yaml`, always re-run codegen before using updated types.
- Do not change the OpenAPI `info.title` — it controls generated filenames.
- Tenant-scoped data: seeding global rows won't appear in the UI because everything is scoped to the signed-in user's auto-provisioned tenant.

## Deferred

- Live social OAuth + real publishing, mobile app. (Billing is live via Razorpay; Stripe not planned.)

## Pointers

- See the `pnpm-workspace` skill for workspace structure, TypeScript setup, and package details
