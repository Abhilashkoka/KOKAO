# KOKAO Composer v2 — rebuilt against your current main

I pulled your latest repo before building. Notable context: your Replit agent had already shipped a `QuickPublishPanel` in the Studio (plus streaming captions, background image jobs, campaigns, post metrics, topic-to-video — nice progress). So instead of shipping a second, parallel publisher, **this patch extends your existing QuickPublishPanel and reuses it as the composer's engine** — one publisher, one validation story, zero drift between Studio and Library.

> The earlier `composer.patch` I sent (sitting in your `attached_assets/`) is now obsolete — discard it. This one replaces it.

## Apply it

```bash
git am composer-v2.patch
```

Frontend-only: no `db push`, no codegen, no new endpoints. Restart the app.

## What it adds

**To your existing QuickPublishPanel** (Studio keeps working exactly as before — everything is additive, optional props):

- A pre-publish checklist powered by your shared `social-limits` helpers, shown *before* the click: "X: will post as a thread of 4 tweets", "LinkedIn: overflow continues in 2 comments", "Threads: chain of 3 posts"
- Instagram gating when the item has no image — disabled checkbox with a "(needs an image)" hint instead of a publish-time failure, and excluded from "Publish to all connected"
- A `beforeAction` hook (used to persist caption edits before the first platform posts) and an opt-in remembered platform selection
- Studio's panel now passes the caption and image state through, so these protections light up there too

**New `ComposerSheet` for the Library:** every card gets one primary **"Publish / Schedule"** button opening a sheet with media preview (image *or* video), inline caption editing (saved before publishing so all platforms post identical text), and the shared panel for platform selection, post-now, publish-to-all, and scheduling. Your last platform picks are remembered across sessions. The five legacy per-platform dialogs remain untouched underneath — removable later once the composer has proven itself.

## Verified against your current main

- Monorepo typecheck clean (api-server, web, mobile — including all your new upstream code)
- 65/65 tests across the composer (7 new), Studio, and Library suites
- No conflicts with `studioQuickPublish`, `campaigns`, or `postMetrics` feature flags
