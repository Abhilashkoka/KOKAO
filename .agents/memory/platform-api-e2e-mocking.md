---
name: Mocking platform APIs for browser e2e runs
description: How to make Threads/X publish flows succeed end-to-end in a real browser session without hitting real platform APIs.
---

**Rule:** Threads and X outbound base URLs support dev-only env overrides (`THREADS_GRAPH_BASE_OVERRIDE` in the threads routes, `TWITTER_API_BASE_OVERRIDE` in the twitter API lib), ignored when `NODE_ENV=production`. Point them at a tiny local HTTP mock that answers the probe (empty `{data:[]}`) and the create/publish endpoints with fake ids, and a browser e2e can exercise real publish/resend flows to completion.

**Why:** The publish/resend routes call graph.threads.net / api.x.com directly; with fake seeded tokens every call fails, so "flow completes and UI clears" can never be verified in a real browser otherwise.

**How to apply:**
- Run the mock as a temporary WORKFLOW, not a nohup/setsid background bash process — background processes are reaped between bash tool invocations and the mock dies mid-test (this caused two false e2e failures).
- The mock must PERSIST its request log to a /tmp file and RELOAD it on startup — validation runs restart all workflows around test runs, and an in-memory-only log (or one truncated at boot) silently loses the "exactly N platform writes" evidence after the run.
- Threads publish flow is fully mockable with just: GET /me, GET /:userId/threads → {data:[]} (dedupe probe), POST /:userId/threads → {id}, POST /:userId/threads_publish → {id}. Publishing needs no app_credentials row — only the connected_accounts row (plaintext access_token, token_expires_at NULL, provider_user_id, verify_status 'verified'). Adding an artificial ~2.5s delay to threads_publish makes the UI's in-flight disabled state observable to a browser tester.
- Set the override env vars in the `development` environment, restart the API server, run the test, then delete the vars, remove the workflow, and restart again.
- Seeding for resend: threads account row needs plaintext `access_token`, `token_expires_at` NULL (skips refresh); twitter row needs `encrypted_credentials` (encryptJson of `{accessToken, refreshToken}`) + future `token_expires_at`, plus a decryptable `app_credentials` provider='twitter' row (snapshot/remove after).
- Tenant is auto-provisioned on first authenticated page load; target it by `tenants.email` = the Clerk test login email, and clean up all seeded rows (children first) afterwards.

**Meta Ads variant:** the Meta Ads adapter AND the Meta Ads OAuth callback token exchange honor `META_ADS_GRAPH_BASE_OVERRIDE` (non-prod only, read at call time via the exported `adsGraphBase()`). Set the override WITHOUT a trailing slash — the adapter appends `/${path}`, and a double slash makes `new URL(req.url, base)` in the mock parse the ad account id as a protocol-relative HOST, so requests arrive with an empty account path (cost one false e2e failure). A reusable stateful mock lives at `scripts/src/metaAdsMockServer.mjs` (ad account act_777001, seeded "Summer Sale" campaign + insights, plus a seeded ad set "Summer Sale - Prospecting" 130000000000001 with start/end_time; adset list/read/update honor `end_time`, campaigns use `stop_time`; persists to /tmp). The full /ads flow (reuse-FB-connection → account pick → campaigns → draft → owner approve → change history) plus the superadmin ads switch were validated in-browser with it. The Meta ad set end-date change flow (edit prefill → draft with "End time" before/after diff → owner approve → applied+verified, one mock write) was also validated in-browser. Simplest connection seeding: pre-insert a template `ad_account_connections` row (tenant_id 0, platform 'meta', encryptJson({accessToken})) and have the test INSERT..SELECT-copy it to the test tenant. Note: the ad set table on the campaign detail has NO schedule column — don't write a verify step expecting the end date there (caused a step-16-only "failure" on an otherwise passing run). The mock's ad sets also carry `bid_amount`/`bid_strategy` (read + update); the Meta ad-set BID flow (edit dialog → no-cap strategy disables the bid-amount input → Cost cap + amount → draft → owner approve → applied+verified, exactly one update_adset with bid_amount/bid_strategy in the mock log) was validated in-browser. The adset edit prefill does NOT preload current bid fields — the strategy select opens on "Keep current"; that's expected. The mock also has OAuth endpoints (`POST oauth/access_token`). The shadcn calendar+time DateTimeField (draft dialog Start/End) was validated in-browser with this mock: popover opens over the Radix dialog, picked dates land in the draft payload as local-offset ISO, prefilled ad-set schedule renders readable and clearing omits stopTime from the payload; approve applied+verified with exactly one mock write. `ad_change_requests` has no `apply_error` column — don't reference it in test-plan SQL.

**LinkedIn Ads variant:** the LinkedIn Ads adapter honors `LINKEDIN_ADS_BASE_OVERRIDE` (non-prod only). Reusable mock at `scripts/src/linkedinAdsMockServer.mjs` (account 511001, starts with ZERO campaign groups, group create via x-restli-id header + read-back; also creatives list `q=criteria`/read/PARTIAL_UPDATE with percent-encoded sponsoredCreative URNs, plus `posts/<urn>` ad-copy previews (content passthrough for media), `images/<urn>` reads, a 1x1 PNG at `/mock-image.png`, plus `adTargetingEntities` facet typeahead with fixed fixtures per facet — locations/industries/jobFunctions/titles; persists to /tmp; run as workflow `PORT=9098 node …`). The expanded facet-targeting flow (Edit targeting dialog → Industries + Job titles typeahead select → draft diff shows "Target industries"/"Target job titles" → owner approve → applied/verified, exactly one campaign update carrying the FULL merged targetingCriteria with untouched locations preserved) was validated in-browser; seed the campaign with an existing locations facet in the state file so the merge is observable. Seed richer fixtures by writing `/tmp/linkedin-ads-mock-state.json` before starting the mock (no template row exists in the DB by default — create the pre-encrypted tenant_id-0 'linkedin' connection template with a temp tsx script using server encryptJson, far-future expiries so no refresh, then delete it after). The DraftDialog group-create flow and the creative Pause AND Archive flows (row buttons → in-app confirm dialog incl. destructive archive wording + cancel path → draft toast → owner Approve-and-apply confirm dialog → applied/verified in ad_change_requests → status reflected on reopen, exactly one mock write per flow) were validated in-browser with it. Seed `creatives`/`posts`/`images` maps in the state file (a creative whose post URN is absent from `posts` exercises the unresolvable-preview path); the campaign-detail creative previews (text-ad-copy-*/img-ad-preview-*, plus the no-preview name-only render for non-image/text) were validated in-browser.

**Google Ads variant:** a stateful mock lives at `scripts/src/googleAdsMockServer.mjs` (OAuth authorize 302 + token endpoint with revocable refresh token via `/__control/revoke`, MCC 9990001 → client 9990002, GAQL search, budget/campaign mutates; persists to /tmp). `buildGoogleAdsAuthUrl` honors dev-only `GOOGLE_ADS_AUTH_URL_OVERRIDE` (plus the existing API base/token overrides), so the browser's real OAuth redirect completes against localhost — the tester's browser CAN reach localhost mock ports directly. Mock token `expires_in: 30` (below the 60s refresh slack) makes every API call refresh, so revocation takes effect immediately → reconnect prompt. Full connect → MCC client pick → campaigns → draft → owner approve → verify → revoke/reconnect flow validated in-browser. The RECOVERY path is also validated: seed a failed connection by copying a pre-encrypted template row (creds = refreshToken + loginCustomerId) with `verify_status='failed'`, revoke the mock, click Reconnect — the mock's authorization_code exchange un-revokes automatically, the callback resets the connection to pending_selection (user re-picks the account; expected UX), and after re-pick campaigns load with verify_status='verified'. Seed `app_credentials` provider `google_ads` via a temp tsx script using server `encryptJson` (snapshot/restore). Clean up the test tenant's `ad_account_connections`/`ad_change_requests`/`ads_change_logs` afterwards or the connection sweep will fail-streak against the dead mock. The mock also serves ad groups ("Morning Pastries" 666000001, cpcBidMicros) and ads ("Fresh Croissants RSA" 888000001) with GAQL WHERE filters plus `adGroups:mutate`/`adGroupAds:mutate` (composite `<adGroupId>~<adId>` resource names); the ad-group pause+CPC-bid and ad pause (disabled rename) draft→approve→change-log flows were validated in-browser with it.

**E2E DB-seeding pitfalls (cost several false failures):**
- Never have the browser-test agent RETYPE a long encrypted blob into SQL — it mistranscribes characters and decrypt fails ("Unsupported state or unable to authenticate data"). Pre-insert a template row (e.g. tenant_id 0, platform 'e2e-template-…') and have the test copy `encrypted_credentials` via a SQL subselect; delete the template afterwards.
- Tenant provisioning is lazy and can race the test's DB seed step: make the plan explicitly POLL `SELECT id FROM tenants WHERE email=…` (up to ~30s) before inserting tenant-scoped rows; "navigate and wait for shell" alone is not enough.
- `tenants.email` is stored LOWERCASED — poll/join with `lower(email)=lower(…)` or use an all-lowercase test email, or the tenant poll returns empty forever (cost a false "unable" run).
- A silent 0-row `INSERT … SELECT FROM tenants WHERE email=…` is the classic symptom — always verify the insert with a count.
- TENANT-SCOPE every e2e DB assertion (join on `tenants.email = <test login>`), never `ORDER BY id DESC LIMIT 1` on a shared table: the `test` workflow's vitest suite writes rows (including deliberate-failure ones like "(#100) Invalid budget") into the same dev DB concurrently and caused a false e2e failure on the ads budget-increase flow. Reusable snippet for browser test plans (works for ad_change_requests, scheduled_posts, content_items, etc.):
  ```sql
  SELECT r.* FROM ad_change_requests r
  JOIN tenants t ON t.id = r.tenant_id
  WHERE lower(t.email) = lower('<clerk test login email>')
  ORDER BY r.id DESC LIMIT 1;
  ```
  Even better: have the tester capture the draft/row id from the UI (URL, dialog, or change-history row) and assert `WHERE r.id = <captured id>` directly.

A reusable TikTok Marketing API mock lives at `scripts/src/tiktokMockServer.mjs` (stateful campaign AND ad group — adgroup/get|update|status/update honor budget + schedule_type/start/end so ad group draft read-back verify passes; campaign/create registers the new campaign and campaign/get honors `filtering.campaign_ids`, so the engine's post-create read-back verify passes; `POST /__control {"revoked":true}` flips business-code-40105 revoked-grant mode; token exchange resets it; log at /tmp/tiktok-mock-log.json). TikTok connect e2e: don't click the Connect button (real tiktok.com portal isn't overridable) — fetch `/api/ads/tiktok/auth/url` in-page, extract the signed `state`, then navigate to the public callback with `auth_code=<anything>&state=...`. Simpler for non-connect flows: pre-insert a tenant_id-0 template `ad_account_connections` row (platform 'e2e-template-tiktok', encryptJson({accessToken, advertiserIds})) and INSERT..SELECT-copy it to the test tenant. The full TikTok ad group pause + daily-budget + start/end-schedule draft→owner-approve→verified→change-history flow was validated in-browser with it (exactly one adgroup/update + one adgroup/status/update in the mock log). The full new-TikTok-campaign flow (Draft new campaign → TikTok objective picker shows TRAFFIC/REACH/etc not OUTCOME_*, Paused + $25/day → owner Approve-and-apply confirm → "applied and verified" toast → applied/verified in ad_change_requests → campaign listed + Verified in change history, exactly one mock create write) was validated in-browser with it.

**LinkedIn organic silent-refresh variant:** `linkedinTokenUrl()` in linkedinApp.ts honors dev-only `LINKEDIN_TOKEN_URL_OVERRIDE` (full URL, used by the organic refresh path). Reusable mock at `scripts/src/linkedinOrganicMockServer.mjs` (POST /oauth/v2/accessToken returns rotated tokens; `POST /__control {"revoked":true}` flips to 400 invalid_grant; persists to /tmp; run `PORT=9097 node …` as a workflow). Seed a near-expiry row (token_expires_at now()+2d, verify_status 'verified') copying `encrypted_credentials` ({refreshToken, refreshTokenExpiresAt}) from a template row via INSERT..SELECT. Validated in-browser: page load silently renews (Connected pill, token advances 60d) and revoked mode flips to failed → "Reconnect needed". App creds: the dev DB has an `app_credentials` provider='linkedin' row that wins over env LINKEDIN_CLIENT_ID/SECRET — the mock ignores client creds so either works. Sweep-only renewal (no page load) is also validated: with the frontend workflow stopped, restart the API server and the boot sweep (~60s after listen) drives the same refresh — exactly one mock token call, row renewed + refresh token rotated. Simplest seeding is a temp tsx script using server encryptJson to insert tenant+row directly (tenants.name is NOT NULL).

**Creative previews (Meta + TikTok mocks):** both `scripts/src/metaAdsMockServer.mjs` and `scripts/src/tiktokMockServer.mjs` now seed one ad WITH a creative (text + thumbnail served from the mock's own `/mock-image.png`, 1x1 PNG) and one ad WITHOUT a creative (null-fallback path); the TikTok mock also handles `/file/image/ad/info/`. The tester's browser loads localhost mock image URLs directly (mixed-content warning, but they load). Campaign-detail creative previews (`text-ad-copy-*`/`img-ad-preview-*` render, no-creative rows name-only) were validated in-browser for both platforms. Note: the TikTok mock normalizes request paths with a TRAILING slash — new handlers must match `/foo/`.

A reusable Threads Graph mock now lives at `scripts/src/threadsMockServer.mjs` (persists log to /tmp/threads-mock-log.json, ~2.5s publish delay) — run it as a workflow with `PORT=9099 node scripts/src/threadsMockServer.mjs` instead of rewriting one.

**Validated outcome (double-click publish guard):** a real-browser e2e with this harness confirmed a rapid double-click on the Library's Publish (dialog + card buttons) AND the failed-item Retry button produces exactly one platform write each (mock log: one probe + one container create + one publish per flow), no 409 "already in progress" toast, and buttons disabled while in flight (ref-based `publishInFlightRef` guard in library.tsx).

**Validated outcome (double-click resend guard):** same harness confirmed a real-browser double-click on the pending-posts "Resend posts" button performs exactly one resend (mock log: 1 probe + one create/publish per missing piece), no 409 toast, button disabled while in flight. React state disabling alone is NOT enough for double-clicks — the resend hook uses a synchronous ref guard that flips before re-render.
